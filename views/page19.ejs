<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Start Your First Run</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    /* Body Styling */
    body {
      font-family: Arial, sans-serif;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      background-color: #f0f0f0;
      background-image: url('/erasebg-transformed (26).png'); /* Dynamically injected image path */
      background-size: cover;
      background-position-y: 0%;
    }

    .container {
      position: relative;
      width: 100%;
      height: 100%;
    }

    .overlay {
      position: absolute;
      top: 60%; /* Move text further down */
      left: 50%;
      transform: translate(-50%, -50%);
      text-align: center;
      color: white;
      background-color: rgba(0, 0, 0, 0);
      padding: 20px;
      border-radius: 10px;
      width: 80%;
      max-width: 600px;
    }

    .overlay h1 {
      font-size: 2.5rem;
      margin-bottom: 20px;
    }

    .action-button {
      padding: 12px 25px;
      background-color: #007BFF;
      color: white;
      border: none;
      border-radius: 5px;
      font-size: 1.2em;
      cursor: pointer;
    }

    .action-button:hover {
      background-color: #0056b3;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      .overlay h1 {
        font-size: 2rem;
      }

      .action-button {
        font-size: 1.1rem;
        padding: 14px 30px;
      }
    }

    @media (max-width: 480px) {
      .overlay h1 {
        font-size: 1.6rem;
      }

      .action-button {
        font-size: 1rem;
        padding: 12px 25px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="overlay">
      <h1>That's it! Let's start your first run!</h1>
      <button class="action-button" onclick="window.location.href='/normal'">Build my Plan</button>
    </div>
  </div>
</body>
</html>
