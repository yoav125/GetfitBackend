<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>GetFit - Let's Start with the Basics</title>
  <style>
    /* General Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    /* Body Styling */
    body {
      font-family: 'Segoe UI', sans-serif;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      background-color: #f4f4f4;
      flex-direction: column;
    }

    /* Hero Section */
    .hero-container {
      position: relative;
      width: 100%;
      height: 100vh;
      background-image: url('/erasebg-transformed (10).png'); /* Updated to new image */
      background-size: cover;
   background-position-y: 13%;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
    }

    .overlay {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(0, 0, 0, 0); /* Dark overlay for text visibility */
    }

    .content {
      z-index: 1;
      width: 100%;
      margin-top: 100px; /* Increased margin to move the text further down */
      text-align: center;
      color: white;
      max-width: 600px;
      padding: 20px;
    }

    h1 {
      font-size: 4rem; /* Adjust font size for header */
      font-weight: bold;
      margin-bottom: 20px;
    }

    p {
      font-size: 2rem; /* Adjust font size */
      margin-bottom: 30px;
    }

    .btn {
      display: block;
      width: 100%;
      max-width: 350px;
      padding: 16px;
      border: none;
      border-radius: 30px;
      font-size: 1.1rem;
      margin: 10px auto;
      cursor: pointer;
    }

    .btn.white {
      background-color: white;
      color: black;
    }

    .btn.blue {
      background-color: #2ca6ff;
      color: white;
    }

    .footer-links {
      position: absolute;
      bottom: 20px;
      left: 50%;
      transform: translateX(-50%);
      display: flex;
      justify-content: space-around;
      width: 100%;
      color: white;
      font-size: 0.9rem;
    }

    .footer-links a {
      text-decoration: none;
      color: white;
    }

    .footer-links a:hover {
      text-decoration: underline;
    }

    /* Progress Bar */
    .progress-container {
      position: fixed;
      bottom: 0;
      left: 0;
      width: 100%;
      height: 5px;
      background-color: #ddd;
      z-index: 2; /* Ensures it's above other content */
    }

    .progress-bar {
      height: 100%;
      width: 30%;
      background-color: #1e90ff;
      transition: width 0.25s ease;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      .content h1 {
        font-size: 3rem;
      }

      .content p {
        font-size: 1.8rem;
      }

      .btn {
        font-size: 1.1rem;
        padding: 14px 30px;
      }
    }

    @media (max-width: 480px) {
      .content h1 {
        font-size: 2.5rem;
      }

      .content p {
        font-size: 1.5rem;
      }

      .btn {
        font-size: 1rem;
        padding: 12px 25px;
      }
    }
  </style>
</head>
<body>
  <div class="hero-container">
    <div class="overlay"></div>
    <div class="content">
      <h1>Let's start with the basics</h1>
      <p>Choose your gender</p>
      <div>
        <a href="/page5" class="btn white">Female</a>
        <a href="/page5" class="btn blue">Male</a>
      </div>
    </div>
    <div class="footer-links">
      <a href="/privacy">Privacy policy</a>
      <a href="/terms">Terms and conditions</a>
    </div>
  </div>

  <!-- Progress Bar -->
  <div class="progress-container">
    <div class="progress-bar" id="progress-bar"></div>
  </div>

  <script>
    // Progress bar functionality
    window.addEventListener("scroll", function() {
      var docHeight = document.documentElement.scrollHeight - window.innerHeight;
      var scrollPosition = window.scrollY;
      var scrollPercentage = (scrollPosition / docHeight) * 100;
      document.getElementById('progress-bar').style.width = scrollPercentage + "%";
    });
  </script>
</body>
</html>
