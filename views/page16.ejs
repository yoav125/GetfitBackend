<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>GetFit - Choose Your Running Days</title>
  <style>
    /* General Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    /* Body Styling */
    body {
      font-family: 'Segoe UI', sans-serif;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      background-color: #f4f4f4;
      flex-direction: column;
      color: white;
    }

    /* Hero Section */
    .hero-container {
      position: relative;
      width: 100%;
      height: 100vh;
      background-image: url('/erasebg-transformed (2).png'); /* Replace with your image path */
      background-size: cover;
      background-position-y: 0%;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
    }

    .overlay {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(0, 0, 0, 0); /* Dark overlay for text visibility */
    }

    .content {
      z-index: 1;
      text-align: center;
      color: white;
      max-width: 600px;
      padding: 20px;
      margin-top: 100px; /* Move text further down */
    }

    h1 {
      font-size: 4rem; /* Increased font size for better visibility */
      font-weight: bold;
      margin-bottom: 20px;
    }

    p {
      font-size: 2rem; /* Adjusted font size */
      margin-bottom: 30px;
    }

    /* Days of the Week Buttons */
    .days-container {
      display: flex;
      justify-content: center;
      margin-bottom: 30px;
    }

    .day-btn {
      background-color: #1e90ff;
      color: white;
      padding: 15px 30px;
      font-size: 1.1rem;
      text-decoration: none;
      border-radius: 5px;
      margin: 5px;
      width: 50px;
      text-align: center;
      transition: background-color 0.3s ease;
    }

    .day-btn:hover {
      background-color: #4682b4;
    }

    /* Time Selector */
    .time-container {
      margin-bottom: 30px;
    }

    .time-btn {
      background-color: #1e90ff;
      color: white;
      padding: 15px 30px;
      font-size: 1.1rem;
      text-decoration: none;
      border-radius: 5px;
      width: 200px;
      text-align: center;
      transition: background-color 0.3s ease;
    }

    .time-btn:hover {
      background-color: #4682b4;
    }

    /* Continue Button */
    .btn {
      background-color: #1e90ff;
      color: white;
      padding: 15px 30px;
      font-size: 1.1rem;
      text-decoration: none;
      border-radius: 5px;
      display: inline-block;
      transition: background-color 0.3s ease;
      margin-top: 30px; /* Added margin for better spacing */
    }

    .btn:hover {
      background-color: #4682b4;
    }

    /* Progress Bar */
    .progress-container {
      position: fixed;
      bottom: 0;
      left: 0;
      width: 100%;
      height: 5px;
      background-color: #ddd;
    }

    .progress-bar {
      height: 100%;
      width: 50%; /* Adjust this based on progress */
      background-color: #1e90ff;
      transition: width 0.25s ease;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      .content h1 {
        font-size: 3rem;
      }

      .content p {
        font-size: 1.8rem;
      }

      .btn {
        font-size: 1.1rem;
        padding: 14px 30px;
      }

      .day-btn {
        width: 40px;
        padding: 10px;
      }

      .time-btn {
        width: 180px;
      }
    }

    @media (max-width: 480px) {
      .content h1 {
        font-size: 2.5rem;
      }

      .content p {
        font-size: 1.5rem;
      }

      .btn {
        font-size: 1rem;
        padding: 12px 25px;
      }

      .day-btn {
        width: 35px;
        padding: 8px;
      }

      .time-btn {
        width: 160px;
      }
    }
  </style>
</head>
<body>
  <div class="hero-container">
    <div class="overlay"></div>
    <div class="content">
      <h1>On what days would you like to run?</h1>
      <p>We recommend running 3 days a week</p>

      <!-- Days of the Week Buttons -->
      <div class="days-container">
        <button class="day-btn">S</button>
        <button class="day-btn">M</button>
        <button class="day-btn">T</button>
        <button class="day-btn">W</button>
        <button class="day-btn">T</button>
        <button class="day-btn">F</button>
        <button class="day-btn">S</button>
      </div>

      <!-- Time Selector -->
      <div class="time-container">
        <button class="time-btn">Set a time: 9:00 AM</button>
      </div>

      <!-- Continue Button -->
      <a href="/page17" class="btn">Next</a>
    </div>
  </div>

  <!-- Progress Bar -->
  <div class="progress-container">
    <div class="progress-bar" id="progress-bar"></div>
  </div>

  <script>
    // Progress bar functionality
    window.addEventListener("scroll", function() {
      var docHeight = document.documentElement.scrollHeight - window.innerHeight;
      var scrollPosition = window.scrollY;
      var scrollPercentage = (scrollPosition / docHeight) * 100;
      document.getElementById('progress-bar').style.width = scrollPercentage + "%";
    });
  </script>
</body>
</html>
